# stack-trail

A terminal chatbot that talks to Claude. Type a message, get a response.

## Setup

```bash
pip install anthropic rich
export ANTHROPIC_API_KEY="your-key"
python chatbot.py
```

## Commands

- `/clear` — start fresh
- `/history` — see the conversation so far
- `/exit` — quit
